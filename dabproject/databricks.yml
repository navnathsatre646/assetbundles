# This is a Databricks asset bundle definition for dabproject.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabproject
  uuid: ebba634e-caa6-4358-9177-34f3d8898d9d

include:
  - resources/*.yml
  - resources/*/*.yml
  

# artifacts:
#   python_artifact:
#     type: whl
#     build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
# variables:
#   catalog:
#     description: The catalog to use
#   schema:
#     description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true

    presets:
      name_prefix: dev_${workspace.current_user.short_name}
      source_linked_deployment: false
    workspace:
      host: https://dbc-26095ccc-327f.cloud.databricks.com/
    # variables:
    #   catalog: assetbundles
    #   schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-26095ccc-327f.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/navnathsatre646@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/navnathsatre646@gmail.com/PROD/.bundle/${bundle.name}/${bundle.target}
    # variables:
    #   catalog: assetbundles
    #   schema: prod
    permissions:
      - group_name: admins
        level: CAN_MANAGE
